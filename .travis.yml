sudo: required
services:
  - docker

before_install:
  - docker build -t charlesayottetrepanier/docker-react -f Dockerfile.dev . 

script:
  - docker run -e CI=true charlesayottetrepanier/docker-react npm run test -- --coverage